# Netlify configuration (ignored by Vercel).
# The Next.js runtime plugin gives App Router, API routes, and middleware support.
[build]
  command = "npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"
